<p>
  <strong><%= t('rooms.room.title') %></strong>
  <%= @room.title %>
</p>
<p>
  <strong><%= t('rooms.room.location') %></strong>
  <%= @room.location %>
</p>
<p>
  <strong><%= t('rooms.room.description') %></strong>
  <%= @room.description %>
</p>
<%= link_to t('links.edit'), edit_room_path(@room) %> |
<%= link_to t('links.back'), rooms_path %>
